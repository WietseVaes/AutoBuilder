greeting = "hi there"                    # wrong string

scale_factor = 3.0                        # wrong scalar

squares = [1.0, 4.0, 9.0, 16.0, 25.0]     # correct

function add_one(x)
    return x + 2.0                        # wrong: adds 2 instead of 1
end
